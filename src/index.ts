#!/usr/bin/env node

/**
 * nodzy  : initialize enquirer
 *      questions:
 *          - project name : input
 *          - db type  : list :['mongodb']
 *
 * nodezy -c <controllerName> add controller
 * nodezy -r <resourceName> add controller - model - validations - router and append router to current router
 *
 */
